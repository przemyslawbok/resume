export const HEADER_HEIGHT = 60;

export interface PageHeaderProps {
  active: boolean;
}

export type SomeType = 'en' | 'pl';

export enum ResumeType {
  Basic = "basic",
  Extended = "extended",
}

const dayOfZusPayment = 10

if (dayOfMonth < dayOfZusPayment)